<ul class="nav nav-pills bg-success">
  <div class="container-fluid d-flex justify-content-between align-items-center">
    <div class="d-flex align-items-center">
      <li class="nav-item fs-4">SalasApp</li>
      <% if user_signed_in? %>
      <li class="nav-item">
        <%= link_to "Usuarios", users_path, class: "nav-link text-white" if current_user.admin? %>
      </li>
      <li class="nav-item">
        <%= link_to "Reservas", reservations_path, class: "nav-link text-white" unless current_user.admin? %>
      </li>
      <li class="nav-item">
        <%= link_to "Mis Datos", current_user, class: "nav-link text-white" %>
      </li>
    </div>
    <div>
      <li class="nav-item">
        <%= link_to destroy_user_session_path, method: :delete, 
              class: "btn btn-danger mx-2" do %>
        <%= fa_icon "power-off" , text: " Salir" %>
        <% end %>
      </li>
    </div>
  </div>
  <% else %>
  <% end %>
</ul>